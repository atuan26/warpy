from warpd.schemas import InputEvent, Screen

grid_width = 0
grid_height = 0
scr = Screen()


def draw_grid(scr, color, sz, nc, nr, x, y, w, h):
    return


def redraw(mx, my, force):
    return


def grid_mode() -> InputEvent:
    return InputEvent()
